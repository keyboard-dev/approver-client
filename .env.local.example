# Local Development Configuration
# Copy this file to .env.local (in the same directory as the app executable)
# to override the default server URLs

# Backend API URL (where remote-oauth-server is running)
# For LOCAL development: Use http://localhost:4000
# For PRODUCTION: Comment out or delete this line
OAUTH_SERVER_URL=http://localhost:4000
